{
  "name": "ethswarm-blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "forestry:preview": "hugo server -F -D -E -w",
    "dev": "tinacms server -c \"hugo server -D\"",
    "build": "tinacms build && hugo"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@tinacms/cli": "^2.0.5",
    "tinacms": "^3.3.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "postcss-cli": "^11.0.0"
  }
}
